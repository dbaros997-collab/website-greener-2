services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: grace_high_school
    volumes:
      - grace_pg_data:/var/lib/postgresql/data

volumes:
  grace_pg_data:
