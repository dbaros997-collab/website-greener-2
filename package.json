{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present --filter \"!./artifacts/mockup-sandbox\" run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "setup:local": "pnpm --filter @workspace/scripts run setup-local",
    "setup:db": "node ./scripts/src/setup-db.mjs",
    "set-admin": "pnpm --filter @workspace/scripts run set-admin",
    "dev:api": "node ./scripts/src/run-dev-api.mjs",
    "dev:admin": "node ./scripts/src/run-dev-grace-admin.mjs",
    "dev:site": "pnpm --filter @workspace/grace-high-school run dev",
    "start": "pnpm --filter @workspace/api-server run start"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
